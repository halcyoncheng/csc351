#ifndef SUPERUTIL_H
#define SUPERUTIL_H

// Whoever imports this file, sees the external variable
extern int visible;

// Whoever imports this file, sees the function
int functionA(void);

// Whoever imports this file, sees the function
int functionB(void);

#endif // SUPERUTIL_H
